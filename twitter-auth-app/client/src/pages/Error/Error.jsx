import React from "react";
import { Typography } from "antd";

const { Title } = Typography;

const Error = () => (
  <div>
    <Title>Error: Your requested page does not exist!</Title>
  </div>
);

export default Error;
